import React from 'react'
import { DocumentListContainer } from '@widgets/documents'

const Documents = () => (
	<section className='documents'>
		<DocumentListContainer />
	</section>
)

export default Documents
